#include <stdio.h>

/**
 * main - Adds two numbers and bring out result.
 *
 * Return: Always 0.
 */
int main(void)
{
	int a, b, sum;

	a = 5;
	b = 10;

	sum = a + b;

	printf("Sum of a and b is: %d\n", sum);

	return (0);
}
